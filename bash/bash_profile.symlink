export DOTFILES=$HOME/.files

# source every env file
find -P "$DOTFILES" -name env.conf -type f | while read env_conf
do
  source $env_conf
done

# source local env
[[ -e ~/.localenv ]] && source ~/.localenv

# vim: ft=sh
