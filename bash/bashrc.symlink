# source every alias file
find -P "$DOTFILES" -name alias.conf -type f | while read alias_conf
do
  source $alias_conf
done

# source local config
[[ -e ~/.localrc ]] && source ~/.localrc

# vim: ft=sh
